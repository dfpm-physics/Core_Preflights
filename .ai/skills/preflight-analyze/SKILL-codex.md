# Codex addendum

- Use the available PDF skill to read the referenced textbook pages when PDF grounding is needed.
- Use safe HTTP or repository tooling available in the current Codex environment; never expose the
  service key in commands, URLs, logs, or conversation output.
- Treat the shared `SKILL.md` as authoritative. This addendum changes tools, not grading policy.
